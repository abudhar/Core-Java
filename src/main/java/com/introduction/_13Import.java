package com.introduction;

import java.util.*;//--it will include All classes/Interface in the Package
import java.io.BufferedInputStream;//-- it will include only specified Class/Interface on the package

public class _13Import {
	/*
	 * Import - import is used to use Predefined/User defined Packages in java file
	 * 
	 * Usage : 
	 * 	- import package.*; --it will include All classes/Interface in the Package
	 *  - import package.classname; -- it will include only specified Class/Interface on the package
	 * 
	 * without import we can use like below,
	 */
	 java.io.BufferedReader br = new java.io.BufferedReader(null);
}
